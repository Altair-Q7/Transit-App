name: sarathy_app
description: Sarathy — Unified Transit Intelligence App (Passenger, Crew, Operator)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  web_socket_channel: ^3.0.1
  geolocator: ^13.0.1
  flutter_secure_storage: ^9.2.2
  provider: ^6.1.2
  go_router: ^14.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true